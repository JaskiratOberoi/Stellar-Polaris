/**
 * Re-exports shared run configuration / events for the bot package.
 * Domain types live in @stellar/shared; LIS types are under ./lis/types.ts.
 */
export type { RunConfig, TestCodeId, WsClientEvent } from '@stellar/shared';
